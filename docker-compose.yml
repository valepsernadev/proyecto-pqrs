services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: sistema_pqrs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d sistema_pqrs"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
